<?xml version='1.0' encoding='UTF-8'?>
<odoo>
  <record id="execute_code_174c7097-f823-4cfd-9b9d-c7a5df3bafc2" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# Periksa apakah field x_name sudah memiliki nilai atau belum.
# Kode hanya akan berjalan jika field tersebut kosong.
if not record.x_name:
  # Mengambil nomor urut berikutnya dari sequence (hasilnya: 'JOB/MAWB/00001')
  sequence = env['ir.sequence'].next_by_code('job.mawb.code')

  # Memastikan sequence dan tanggal pembuatan ada
  if sequence and record.create_date:
    # Mengubah format tanggal pembuatan menjadi DDMMYYYY
    date_str = record.create_date.strftime('%d%m%Y')

    # Menggabungkan nomor urut dengan tanggal
    record['x_name'] = f"{sequence}/{date_str}"]]></field>
    <field name="model_id" ref="air_mawb_2430bd5f-f5db-420f-8425-2b1aa6074411"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_86d63b73-65df-4222-b744-93017f6dff13" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# Periksa apakah field x_name sudah memiliki nilai atau belum.
# Kode hanya akan berjalan jika field tersebut kosong.
if not record.x_name:
  # Mengambil nomor urut berikutnya dari sequence (hasilnya: 'JOB/MAWB/00001')
  sequence = env['ir.sequence'].next_by_code('house.awb.seq')

  # Memastikan sequence dan tanggal pembuatan ada
  if sequence and record.create_date:
    # Mengubah format tanggal pembuatan menjadi DDMMYYYY
    date_str = record.create_date.strftime('%d%m%Y')

    # Menggabungkan nomor urut dengan tanggal
    record['x_name'] = f"{sequence}/{date_str}"]]></field>
    <field name="model_id" ref="air_hawb_956eb343-a726-487b-9b68-9efb21cd6ead"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_75939ed8-e547-4858-8744-e7dd24263019" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# Periksa apakah field x_name sudah memiliki nilai atau belum.
# Kode hanya akan berjalan jika field tersebut kosong.
if not record.x_name:
  # Mengambil nomor urut berikutnya dari sequence (hasilnya: 'JOB/BOOK/00001')
  sequence = env['ir.sequence'].next_by_code('booking_job_no')

  # Memastikan sequence dan tanggal pembuatan ada
  if sequence and record.create_date:
    # Mengubah format tanggal pembuatan menjadi DDMMYYYY
    date_str = record.create_date.strftime('%d%m%Y')

    # Menggabungkan nomor urut dengan tanggal
    record['x_studio_job_no'] = f"{sequence}/{date_str}"]]></field>
    <field name="model_id" ref="air_booking_70624282-4cb9-43da-a9d5-185234be2592"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_6bf6c868-5ad4-46db-8c8c-90a59ec7806f" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# Periksa apakah field x_name sudah memiliki nilai atau belum.
# Kode hanya akan berjalan jika field tersebut kosong.
if not record.x_name:
  # Mengambil nomor urut berikutnya dari sequence (hasilnya: 'WPC/BOOK/00001')
  sequence = env['ir.sequence'].next_by_code('booking_no')

  # Memastikan sequence dan tanggal pembuatan ada
  if sequence and record.create_date:
    # Mengubah format tanggal pembuatan menjadi DDMMYYYY
    date_str = record.create_date.strftime('%d%m%Y')

    # Menggabungkan nomor urut dengan tanggal
    record['x_name'] = f"{sequence}/{date_str}"]]></field>
    <field name="model_id" ref="air_booking_70624282-4cb9-43da-a9d5-185234be2592"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_3bdfa0d4-467a-4c56-a52d-6b2ad5dc05b8" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# Cek apakah field NIK diisi
if record.x_name:
  # Cari record lain (selain record yang sedang disimpan) yang memiliki NIK yang sama
  # "id != record.id" penting agar tidak error saat mengedit record tanpa mengubah NIK
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_port'].search_count(domain)
  
  # Jika ditemukan record lain dengan NIK yang sama (jumlah > 0)
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Port Code "%s" sudah digunakan.' % record.x_name)]]></field>
    <field name="model_id" ref="port_b4d0be46-e179-4b2b-9ead-b70e132ac4d7"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_456eb9e4-d668-4712-8870-f3b53345d5e6" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_area'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Area Code "%s" sudah digunakan. Harap gunakan Area Code yang unik.' % record.x_name)
]]></field>
    <field name="model_id" ref="area_3a63d8f4-8695-48eb-99ad-ad6644978a2c"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_fb8ce416-b9c0-457a-aa50-548f12590150" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_airport'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Airport Code "%s" sudah digunakan. Harap gunakan Airport Code yang unik.' % record.x_name)
]]></field>
    <field name="model_id" ref="airport_0713a63a-0498-4e57-b9e4-e6c3d93176b2"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_6eaa96ad-3a0a-424b-afc2-2ca9e34d1ff2" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_airlines'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Airline Code "%s" sudah digunakan. Harap gunakan Airline Code yang unik.' % record.x_name)
]]></field>
    <field name="model_id" ref="airlines_ae4ec938-cdb4-4b1b-a981-c7515ecfc2d5"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_f03cb6dc-dc99-4854-9757-e5173bbd0e2d" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_cargo_class'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Cargo Class Code "%s" sudah digunakan. Harap gunakan Cargo Class Code yang unik.' % record.x_name)
]]></field>
    <field name="model_id" ref="cargo_class_b8372d1e-3559-47cd-9161-398492198785"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_6ed7929f-2b4d-444e-82ca-ece28213ca83" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_type_container_box'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Container Type Code "%s" sudah digunakan. Harap gunakan Container Type Code yang unik.' % record.x_name)]]></field>
    <field name="model_id" ref="type_container_box_293a4e48-a2c1-41dc-8728-5fa7178ef878"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_e68a731b-008c-495c-9fb2-153adbc9c2b9" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_delivery_type'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Delivery Type Code "%s" sudah digunakan. Harap gunakan Delivery Type Code yang unik.' % record.x_name)]]></field>
    <field name="model_id" ref="delivery_type_eccf0141-f56d-46b9-8b55-d71cd8a214ae"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="sequence">6</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_b6508022-79dd-4051-805f-9c828364352e" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_commodity'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Commodity Code "%s" sudah digunakan. Harap gunakan Commodity Code yang unik.' % record.x_name)]]></field>
    <field name="model_id" ref="commodity_efbc7a0b-98f6-45d5-af9b-ef7b95b8e91f"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_77ff590f-bc6d-4902-be39-bb4387e66eeb" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_term_condition'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Trx No "%s" sudah digunakan. Harap gunakan Trx No yang unik.' % record.x_name)]]></field>
    <field name="model_id" ref="term_condition_e0fb1422-8d98-4723-a5ad-4ff06f8ebb57"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_036c8214-6b2a-4823-8a28-e22aaec8be33" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_vessel'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Vessel Code "%s" sudah digunakan. Harap gunakan Vessel Code yang unik.' % record.x_name)]]></field>
    <field name="model_id" ref="vessel_381e0803-1656-4030-96e0-28455979d997"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_ebec7bb0-b62e-402d-89b5-6732f9bbfa7f" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_footer_remark'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Trx No "%s" sudah digunakan. Harap gunakan Trx No yang unik.' % record.x_name)]]></field>
    <field name="model_id" ref="footer_remark_e728ba72-dee0-4bd4-84ee-be45440db492"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_30f6969e-8bd5-45b9-adaf-dbc38f7ae79e" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_shipping_line'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Shipping Line Code "%s" sudah digunakan. Harap gunakan Shipping Line Code yang unik.' % record.x_name)]]></field>
    <field name="model_id" ref="shipping_line_ad9ffa40-ad62-4750-bc56-eee68870f056"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_2596d07e-a2b7-45e5-a73b-04d9db64e8d0" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if record.x_name:
  domain = [
      ('id', '!=', record.id),
      ('x_name', '=', record.x_name)
  ]
  
  # Hitung jumlah record yang cocok
  duplicate_count = env['x_dg_class_code'].search_count(domain)
  
  if duplicate_count > 0:
    # Tampilkan pesan error kepada pengguna
    raise UserError('Area Code "%s" sudah digunakan. Harap gunakan Area Code yang unik.' % record.x_name)]]></field>
    <field name="model_id" ref="dg_class_code_241b9930-8103-401b-b7d6-7d88ff837ce4"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_507f78a3-ecb7-45b2-9017-1f9313ccd591" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[if not record.x_name:
  # Memanggil 'pabrik nomor' dengan kode rahasia 'mawb_job_no'
  # untuk mendapatkan nomor unik berikutnya.
  generated_no = env['ir.sequence'].next_by_code('sea_booking_no')

  # Memasukkan nomor yang sudah jadi ke field 'Job No.'.
  record['x_name'] = generated_no]]></field>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_94986c2b-487c-4ce6-bdc3-bc535a10b331" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# if not record.x_studio_sea_job_no:
#   generated_no = env['ir.sequence'].next_by_code('sea_job_no')

#   # Memasukkan nomor yang sudah jadi ke field 'Job No.'.
#   record['x_studio_sea_job_no'] = generated_no]]></field>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_2bc1c67a-035d-47a7-820d-bf68c87c1865" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# Ganti nama field-nya kalo beda
record['x_studio_company_id'] = env.company.id
record['x_studio_currency_id'] = env.company.currency_id.id]]></field>
    <field name="model_id" ref="sea_pickup_info_4295357b-e2d6-48ee-97a1-a4b55dc14110"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_0812ab57-e336-4c7a-bc25-e5ae984ec009" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# Ganti nama field-nya kalo beda
record['x_studio_company_id'] = env.company.id
record['x_studio_currency_id'] = env.company.currency_id.id]]></field>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_44791dc0-b1d5-48de-9e0b-01f756e094b4" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# 'record' adalah kontak yang sedang dibuat atau diubah
if record.name and record.email:
  
  # Siapkan domain pencarian untuk duplikat
  domain = [
      ('name', '=', record.name),
      ('email', '=', record.email)
  ]
  
  if record.id:
      # Jika ini adalah 'UPDATE', jangan hitung record ini sendiri
      domain.append(('id', '!=', record.id))

  # Cek ke database
  duplicates = env['res.partner'].search_count(domain)
  
  if duplicates > 0:
      # FIX: Menggunakan UserError,
      # yang seharusnya menghasilkan pesan error "cantik"
      raise UserError("Error Validasi: Kontak dengan nama '%s' dan email '%s' sudah terdaftar." % (record.name, record.email))]]></field>
    <field name="model_id" ref="base.model_res_partner"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_d1263483-56a3-4766-8199-13114d64369b" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[
if not record.x_name:
    
    # 1. Ambil Tanggal Sekarang
    # Panggil datetime.datetime langsung
    now = datetime.datetime.now()
    bulan = now.strftime("%m") # Output: 12
    tahun = now.strftime("%y") # Output: 23
    
    # 2. Logic Job Type (FCL/LCL/CONSOL)
    # Value dari PDF: 'Export Job FCL', 'Import Job Consol', dll [cite: 77, 80]
    job_code = "GEN" 
    if record.x_studio_job_type:
        # Ambil kata terakhir ('FCL', 'LCL', 'CONSOL')
        job_code = record.x_studio_job_type.split(' ')[-1].upper()
        
    # 3. Panggil Sequence
    # Pastikan code sequence 'x_hbl.sequence.custom' sudah dibuat di Settings
    seq = env['ir.sequence'].next_by_code('bl_no') or '001'
    
    # 4. Rakit Format: 12/FCL-JKTPUS/23001
    bl_number = f"{bulan}/{job_code}-JKTPUS/{tahun}{seq}"
    
    # 5. Simpan value
    record['x_name'] = bl_number]]></field>
    <field name="model_id" ref="hbl_60240adb-f896-472a-9df0-0e797fe9ee60"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
  <record id="execute_code_2b1da07c-4078-4e82-a291-c7e3458f2559" model="ir.actions.server" context="{'studio': True}">
    <field name="code"><![CDATA[# Ganti 'x_studio_job_no' dengan nama field Job No kamu yang sebenarnya
if not record.x_studio_job_no:
    
    # 1. Ambil Tanggal Sekarang
    # Gunakan datetime.datetime secara langsung
    now = datetime.datetime.now()
    bulan = now.strftime("%m") # Output: 12 (Bulan)
    tahun = now.strftime("%y") # Output: 23 (2 digit Tahun)
    
    # 2. Panggil Sequence Khusus Job
    # Pastikan code-nya sama dengan yang dibuat di Step 1 ('x_hbl.sequence.job')
    seq = env['ir.sequence'].next_by_code('bl_job_no') or '001'
    
    # 3. Rakit Format: 12/JOB-SHEET/23001
    # Format: {Bulan}/JOB-SHEET/{Tahun}{Sequence}
    # String 'JOB-SHEET' kita tulis statis di sini
    final_job_no = f"{bulan}/JOB-SHEET/{tahun}{seq}"
    
    # 4. Simpan ke field
    record['x_studio_job_no'] = final_job_no]]></field>
    <field name="model_id" ref="hbl_60240adb-f896-472a-9df0-0e797fe9ee60"/>
    <field name="state">code</field>
    <field name="name">Execute Code</field>
    <field name="usage">base_automation</field>
  </record>
</odoo>
